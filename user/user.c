#include <stdio.h>
#include <sys/syscall.h>

int umain(void) {
    // char* mymsg = "HELLO THERE";
    // init_syscall();
    // char* buff = "HELLOOOO\n";
    // syscall(sys_printf, buff);
    // syscall(sys_getchar);
    // printf("%s\n", mymsg);
    while (1) {}
    return 0;
}

